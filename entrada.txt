dim i as integer;

proc f(i as integer)
end proc;

function imprime(n as integer) as integer
	print n;
	return 1111;
end function;

proc main()
	read i;
	
	print imprime(i);
	if i then
		imprime(i);
	end if;
end proc;
